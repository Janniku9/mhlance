---
import { currentGame } from '../stores/GameStore';
import type { GameType } from '../types/GameType';

const { game } = Astro.params;
if (game) {
  currentGame.set(game as GameType);
}
---

<!doctype html>
<html lang="en" class="h-full">
  <head>
    <link href="https://vercel.com/font/mono" rel="stylesheet" />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/j.ico" />
    <meta name="generator" content={Astro.generator} />
    <title>J9MH</title>
  </head>
  <body class="min-h-full font-mono m-0 bg-background text-text">
    <slot />
  </body>
</html>
